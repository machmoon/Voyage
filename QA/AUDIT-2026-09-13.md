# Overnight audit, September 13 to 14, 2026

Scope: every UI tour in `VoyageUITests` run against the 1.1.1 tree (default text size,
accessibility text size, light mode, connections and layovers, diversion, arrival, all
nine seat maps), a static sweep for swallowed errors, and a comparison with FocusFlight.
Captures are in `QA/`; contact sheets used for review are in `QA/review/`.

## Fixed tonight (in 1.1.1, build 6)

| Finding | Evidence | Fix |
| --- | --- | --- |
| Screen auto-lock diverted every flight the traveler did not keep touching. Nothing disabled the idle timer, so the default 30 s to 1 min auto-lock backgrounded the app and the 30 s grace period diverted the flight. | No `isIdleTimerDisabled` anywhere in `Voyage/` | `RootView` keeps the screen awake for `inFlight` and `layover` only, scoped the way Monal scopes it to a call. |
| Boarding pass print handoff showed a ghost ticket and three empty line feeds. | `QA/printframes` capture (deleted; see commit 0e74770) | One view identity for the whole print, masked at the slot, offset by measured height. |
| In-flight bar wrapped mid-word ("Wi n", "Exi t") on connecting flights at default size, and at large text everywhere. | `e2e-lay-02-leg1-inflight.png` before the fix | `ViewThatFits`: full labels, else icons only. |
| Map Route/Follow and Map/Satellite pickers wrapped mid-word at large text. | `e2e-ax-08-inflight-map.png` before | `ViewThatFits`: side by side, else stacked. |
| Seat map Continue button truncated to "Conti…" at large text. | `e2e-ax-04-seat-selected.png` before | Button keeps its label; the focus-block column yields. |
| Third bag field looked cut off under the footer at large text. | `e2e-ax-05-checkbag.png` | Bottom fade on the list. |
| Runway motion in the window finished early and the plane sat still until rotation. The window used a fixed 34 s / 13 s roll while the schedule rolls 32 to 48 s per aircraft and 28 s under short flights. | Preview frames; `FlightVisualEngine.make` vs `FlightSession.takeoffRollDuration` | Window and airport world take the leg's own schedule. |
| A stale interrupted-flight record put a "Flight diverted" alert over onboarding and over the Settings button. | `e2e-01-onboarding-page1.png` before; E05 failure | Recovery waits for onboarding and skips QA launches. |
| A flight ended inside its first minute filled the logbook with "DIVERTED · 0m" rows. | `qa-13-logbook.png` before | Sub-minute unfinished flights are shown on the diverted screen but not logged; same floor for recovery. |
| Live Activity request and Metal pipeline failures were swallowed. | `try?` sweep | Logged under `com.patrickliu.voyage`. |
| Five UI tours failed on stale labels or missing onboarding-skip flags. | `uitests.log` | Tests updated; `-VoyageRecorderDemo` and `-VoyageDemoFlight` skip onboarding. |

## Where it shipped

1.1.1 (build 6) is waiting for review as submission `12b0ac05-da2d-4715-a61d-583a7396149f`,
with the app preview on the 6.9", 6.5" and 6.1" slots. Build 5 was withdrawn twice: once
for the screen-awake and large-text fixes, once for the runway kinematics fix. The README,
`machmoon/Voyage-QA` (captures and the preview clip) and the personal site were updated
the same night.

## Queued for 1.1.2 (on main, not in the build under review)

Pure mode, the Surprise me card, the accessibility fixes to the boarding pass labels and
the home origin line, and the AirportWorldSceneView removal. Cutting build 7 now would
mean a third withdrawal of 1.1.1 from review, so these wait for 1.1.1 to clear.

## Screenshots

`AppStore/screenshots/` now holds five composited 6.9" screenshots in the order the listing
audit asked for: study window, route, boarding pass, passport, diversion. App Store Connect
refused to replace the sets on 1.1.1 ("Can't Create Screenshot Set while In Review"), so
`build/push_screenshots.py --version-id <1.1.2>` runs against the next version.

## Code review pass (02:30)

A high-effort review of the night's commits confirmed eight findings, all fixed in
`e18a199`: the diverted screen claimed a sub-minute flight was logged; pure mode left the
clock tap and info pill live; `-VoyageSceneHour` missed golden hour; landing braking still
used the static duration; the preview tour ended before rotation and its comments described
a roll that no longer exists; the recorder left the status-bar override behind on failure;
the launcher raised from a Tk trace on a blank hour; and a 2.1 MB image set was orphaned.

## Video review (03:00 to 03:40)

Screen recordings (`xcrun simctl io booted recordVideo`, frames pulled at 10 fps) of the tear,
the departure, and a full demo leg. Two fixes came out of it: the stub fell in three frames
and the curtain cut in before the tear registered (now 0.55 s fall, 0.95 s hold, 0.7 s
dissolve), and the drawn world flashed green between the dark pane and the satellite imagery
(the loading cover now sits under the map). Recorder caveat: `recordVideo` writes bunched
timestamps, so normalize to constant frame rate before reading frame times
(`ffmpeg -vf fps=30`), and kill any stale `Voyage.app/Voyage` process before recording; a
leftover instance without launch arguments was driving two recordings. `DemoReelUITests`
now has `testLandingCloseUp` and `testWebsiteDemo` for recording the arrival flow.

## Still open

- **Home cards at accessibility sizes** still truncate city names ("Los An…", `e2e-ax-01-home.png`); the code above each card carries the meaning. The boarding pass labels and the header origin line are fixed (labels tighten to 0.5, the header drops miles then the city name before truncating).
- **Full UI suite on main**: every tour passes after the open-logbook identifier and `-VoyageEmptyLogbook` fixes (recorder captures were reading the simulator's real logbook).
- **Cabin-service capture tour** now passes (three cases, about four minutes each) and writes `QA/cabin-service/`. CI is green on `ad4aeca`.
- **App preview** ends on the takeoff roll. Rotation is about 21 s after the tear and booking takes 10 s, so it cannot share the 30 s with the globe opening; the globe wins.
- **`AirportWorldSceneView`** is not constructed anywhere; it still reads the static roll constants. Dead code candidate.
- **Inline hex colors**: 48 in `WindowSceneView` (scene art, fine) and 17 in `ArrivalFlowView` (passport palette). Worth moving the passport set into `Theme` when that screen is next touched.

## Against FocusFlight (App Store, 4.9 from 9,300 ratings, Editors' Choice)

What they have that Voyage does not:

- **App blocking through Screen Time.** Voyage's listing promises not to block other apps; keep that, but it is the single most-cited reason people pay for FocusFlight.
- **Pure mode**: done on main (double tap the flight screen; chrome fades, VoiceOver exempt). Ships in 1.1.2.
- **Random route**: done on main ("Surprise me" die at the end of the destination rail). Ships in 1.1.2.
- **Focus music** alongside white noise; reviewers complain their ocean and rain tracks are harsh, which is an opening for Voyage's generated cabin ambience.
- **Meal-break requests** on long sessions, which Voyage already answers with layovers and cabin service.

What Voyage has that they do not: real block times and carriers, seat choice with a real window side, the torn pass, layovers, passport stamps, trip replay, Live Activity, weather in the window, and no subscription (FocusFlight is $3.99 a month or $18.99 a year).

## How to reproduce the sweep

```bash
xcodebuild -project Voyage.xcodeproj -scheme Voyage \
  -destination 'platform=iOS Simulator,name=iPhone 17' test -only-testing:VoyageUITests
python3 - <<'EOF'   # contact sheets
from PIL import Image; import glob
fs = sorted(glob.glob('QA/e2e-ax-*.png')); ims = [Image.open(f) for f in fs]
w, h = ims[0].size; s = 0.3; sheet = Image.new('RGB', (int(w*s)*len(ims), int(h*s)))
for i, im in enumerate(ims): sheet.paste(im.resize((int(w*s), int(h*s))), (i*int(w*s), 0))
sheet.save('QA/review/ax.png')
EOF
```
