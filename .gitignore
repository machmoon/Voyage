# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# Cloudflare worker dependencies
worker/node_modules/

# Playgrounds
timeline.xctimeline
playground.xcworkspace

# Local QA log dumps and generated captures (committed media lives in Voyage-QA)
QA/*.log
QA/*.txt
QA/uitest/
QA/**/*.png
QA/**/*.jpg
QA/**/*.jpeg
QA/**/*.mp4
QA/**/*.mov
QA/**/*.gif
QA/**/*.webp
QA/demo/
QA/gallery/
QA/voice-candidates/

# Private submission prep — kept locally, never pushed
BUILD_WEEK_DEMO.md

# App Store Connect signing keys. Never commit these. The pipeline reads the
# key from ~/.appstoreconnect/private_keys/, not from the repo, so a copy here
# is pure exposure.
private_keys/
*.p8

# Agent worktrees are local scratch, never part of the repo
.claude/worktrees/
